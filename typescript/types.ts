// declaring that today is going to have a Date type
const today = new Date();
// typing today. will now show all available methods for Dates
today.getMonth();

const person = {
    age: 20,
};

class Color {}
const red = new Color();
